# 助教系统源码仓库规范（AGENTS.md）

本文件只约束 `C:\助教系统\deploy_zhujiao` 这个 Git 仓库。整个工作区的通用安全、资料、反馈输出和归档规则见上级 `C:\助教系统\AGENTS.md`；系统当前事实、进度和历史见本目录 `PROJECT.md`。

## 一、仓库边界与事实来源

- Git 根目录是 `C:\助教系统\deploy_zhujiao`；上级 `C:\助教系统` 不是 Git 仓库。
- 本目录是提交 GitHub、触发 Vercel 部署的源码主目录。
- 线上生产地址：`https://zhujiao1111.vercel.app`。
- 线上数据事实标准是 Supabase；禁止读取、修改或依赖废弃的本地 `data/*.json`、`state.json` 作为线上事实。
- 学员姓名、电话、快照和其他敏感数据不得写入前端静态文件、Git 或未归档临时产物。
- 云端探测和验证脚本统一复用 `tools/_lib.py` 的登录、请求、bootstrap 重试和学员查询能力；新脚本不得复制登录模板。

## 二、代码分工（禁止回退为单文件）

- `js/modules/dashboard.js`：业务页面、表格、筛选和交互。
- `js/core.js`：底层通讯、弹窗、导航和通用工具。
- `api/[...route].js`：后端 API 路由与 Supabase REST 访问。
- `app.js`：仅保留启动逻辑，不塞业务代码。
- `index.html`：页面容器与静态入口，复杂业务逻辑放模块文件。
- `style.css`：样式与响应式布局。
- `supabase/`：数据库 schema、迁移 SQL 和可追溯的数据结构变更。
- `tools/`：审计、验证和一次性运维脚本；敏感快照、测试报告和中间产物按上级规范归档。

修改前精准阅读与需求直接关联的文件；不要全盘通读、重写大文件或顺手重构无关模块。

## 三、业务与数据操作红线

1. 删除学员、订单、档案等操作遵守上级 AGENTS 的审批红线；未获老板拍板不得执行。
2. 不得用修改自动分层字段的方式伪造人工分级；人工覆盖必须写入专用字段。
3. 业务事实（请假、跟进、反馈、调课、转班、分级变更和审计记录）不得因待办完成、取消或归档而物理删除。
4. 新增数据库字段或表必须提供可回滚、可追溯 SQL；涉及 Supabase 管理权限时交付 SQL 和验证步骤，不虚报已执行。
5. 临时运维端点用完必须删除，并 grep 确认没有同名残留路由；禁止把临时接口当长期能力。

## 四、标准开发闭环

### 1. 开始前

- 读取 `PROJECT.md`，确认当前提交基线、线上状态、未完成项和待老板拍板事项。
- 读取本文件，确认代码边界与验证要求。
- 记录当前 `git log -1` 和 `git status --short`；发现已有用户改动时与其共存，不擅自覆盖。
- 明确本次需求对应的 PRD、源码文件、数据库变更和验收标准。

### 2. 实施中

- 只修改需求涉及的文件。
- 云端脚本先设置 UTF-8 编码并使用公共库；外网访问按项目既有代理方式执行。
- 删除、批量写入、迁移和真实数据验证必须留痕；禁止用真实学员做无留痕写入测试。
- 不把姓名、电话、快照或临时导出放在仓库根目录；中间产物统一放 `_archive/日期_任务名/`。

### 3. 验证与上线

- 至少完成与改动匹配的 JS/Node 语法检查和功能验证。
- 涉及 API、云端数据或部署时，运行对应 `tools/` 验证脚本；只读静态检查不能冒充线上验收。
- 只添加明确指定的文件，禁止 `git add .`，避免把 `tools/`、`data/` 和敏感产物带入提交。
- 未获老板明确授权不得自动提交或推送；若任务已明确要求上线，按以下顺序执行：
  ```bash
  cd deploy_zhujiao
  git add <明确指定文件>
  git commit -m "feat/fix: 简要描述"
  git push origin main
  python tools/_verify_deploy.py
  ```
- 线上验证必须报告实际结果、验证时间和未完成/阻塞项，不得只说“页面能打开”或“接口返回成功”。

### 4. 收尾（强制）

凡代码、数据库、部署配置、线上数据口径、业务规则或验收状态发生变化，任务未完成以下动作不得结束：

1. 更新 `PROJECT.md`：顶部更新时间、最新 commit（若有提交）、对应的已完成/未完成/下一步/已修复记录；云端数字注明采集时间和来源脚本，未重新采集就明确写明。
2. 检查 `PROJECT.md` 中标记为“已提交/已上线/已验证”的内容与实际 Git、线上结果一致；不确定的标记为“待验证”，不得写成完成。
3. 运行 `git status --short`，处理或明确记录剩余未跟踪、未提交文件。
4. 若变更的是仓库长期开发规则，再更新本文件；不要把易变的数字、临时结果复制进 AGENTS。

只改代码不更新 PROJECT，视为任务未完整收尾。PROJECT 是事实账本，不是用来替代本文件的行为规范。

## 五、项目专用规则

- `TERM` 按项目既有北京时间规则处理，不能自行硬编码换季逻辑。
- 待办、分层、反馈和请假等功能必须区分业务事实与工作队列；不得用自由文本、首页名单或删除历史数据替代结构化业务状态。
- 费用/欠费是否进入本期工作队列，以当前有效 PRD 和 PROJECT 为准；不得在未确认范围时新增催收流程。
- 移动端和桌面端均属于验收范围；响应式改动需覆盖窄屏表格、弹窗、筛选和返回操作。

## 六、脚本和错误自愈

脚本因引号、编码、缩进、路径或平台语法报错时，不得中途放弃；立即改用独立脚本文件执行（Windows Git Bash 可使用 `cat << 'EOF' > script.py`），修正后继续跑到最终结果。错误、重试和最终验证结果应写入交付记录或 PROJECT 对应章节。
